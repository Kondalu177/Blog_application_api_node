const authController = require("./auth");
const categoryController = require("./category");
const uploadController = require("./file");
const postController = require("./post");

module.exports = {
  authController,
  categoryController,
  uploadController,
  postController,
};
